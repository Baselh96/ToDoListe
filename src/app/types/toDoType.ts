export declare type ToDo = {
    id: UUID;
    title: string;
    categorie: string;
    status: string;
    description: string;
    date: string;
    done_date: string;
};

export type UUID = string;